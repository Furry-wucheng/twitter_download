# Runtime dependencies for the Python fallback in start.bat/start.sh.
# Python >= 3.13 is required. uv users should prefer `uv sync --locked`.
httpx>=0.28.1,<0.29
PySide6>=6.8,<7
XClientTransaction>=1.0.2,<2
